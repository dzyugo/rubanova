# P0 TODO

- [ ] Stabilize release gate (format/lint pass in CI)
- [ ] Fix cart shipping behavior to match checkout defaults
- [ ] Remove hardcoded Supabase fallback credentials
- [ ] Re-run build and lint checks and report blockers
